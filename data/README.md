# Data

The original SCOPILE VIRAL YouTube Analytics export is not included in this public repository.

To reproduce the workflow with your own data, export a compatible YouTube Analytics table and save it as `Table data.csv` in the project root.

Raw CSV files are excluded by `.gitignore` to avoid accidentally publishing channel-level analytics data.
